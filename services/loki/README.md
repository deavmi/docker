loki
====

## Networks

A Docker userland proxy TCP port is setup so as to map the host
side's port of `3100` to the container side's port `3100`.

## `.env`

Please set the following interpolation variables:

TODO: Just assume `/etc/loki/loki.yaml`

<!-- 1. `CONFIG_FILE` -->
	<!-- * Path to the config file to use when starting Loki -->
