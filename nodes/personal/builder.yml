secrets:
  BUILDER_TOKEN:
    file: builder.secret

services:
  builder:
    extends:
      file: ../../services/builder/compose.yml
      service: builder
    container_name: builder
    restart: unless-stopped
    networks:
      - mainNet
