secrets:
  MONERO_NODES:
    file: nodes.monero

services:
  monero:
    extends:
      file: ../../services/monero/compose.yml
      service: monero
    env_file:
      - path: monero.secrets
        required: true
    networks:
      - monnet
    ports:
      - 18080:18080/tcp
      - 18081:18081/tcp
