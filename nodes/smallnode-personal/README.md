smallnode
=========


## Setup

### Ntfy

Please create `.ntfy.secrets` and set `NTFY_AUTH_USERS`
to `user:pass` and escape `$` with two `$$` (if you
use [them](https://docs.ntfy.sh/config/#example-config))
