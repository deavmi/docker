networks:
  mainNet:
    driver: bridge
    name: mainNet
    enable_ipv6: true
    ipam:
      config:
        - subnet: fde4:492b:fc7f::/48

  grafana_db_net:
    driver: bridge
    name: grafana_db_net
    enable_ipv6: true
    ipam:
      config:
        - subnet: fde4:492c:fc7f::/48
